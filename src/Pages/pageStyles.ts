export const pageStyles = () => {
    return {
        pageContainer: {
            position: 'absolute' as 'absolute',
            "margin-top": `${window.innerHeight * .05}px`,
            height: `${window.innerHeight * .95}px`,
            width: '100%',
        }
    }
}